A GNUstep preference pane for global keyboard shortcuts on FreeBSD systems.

Configure custom keyboard shortcuts for launching applications. The
shortcuts are carried out by Workspace, which reads them from the
GlobalShortcuts defaults domain.
